# iCards MCP Configuration
# Copy this file to .env and fill in your values

# Scope: local, prod
# Default is local (no se necesita configurar por defecto)
# SCOPE=local

# API Configuration (HTTP)
# El MCP se comunica con la API REST por HTTP
API_BASE_URL=http://localhost:3000

# Authentication token (JWT) for API access
# Required for all API operations - get it by logging in:
# curl -X POST http://localhost:3000/api/auth/login \
#   -H "Content-Type: application/json" \
#   -d '{"username": "your-username", "password": "your-password"}'
# Copy the "token" field from the response
AUTH_TOKEN=your_jwt_token_here

